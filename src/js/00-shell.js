  const canvas = document.getElementById("gameCanvas");
  const ctx = canvas.getContext("2d");
  
  // ============================================================================
  // PACTO DE ENGENHARIA OCULTO (LEMBRETE PERMANENTE)
  // "De agora em diante, ao lidar com bugs que quebram a imersão (como áudio 
  // estourando ou física quebrando a tela), pular as abordagens 'puristas e 
  // brandas' e aplicar direto a solução de blindagem definitiva e absoluta, 
  // para não perdermos tempo."
  // Parede de concreto primeiro. A imersão do jogador não pode quebrar.
  // ============================================================================

